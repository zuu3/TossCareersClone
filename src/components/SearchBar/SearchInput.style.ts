import styled from '@emotion/styled'

export const InputWrapper = styled.div`
  display: flex;
  align-items: center;
  background: #fff;
  padding: 12px 16px;
  border-radius: 8px;
  box-shadow: 0 1px 4px rgba(0,0,0,0.1);
  flex: 1;
  min-width: 320px;
  max-width: 520px;
  svg { color: #adb5bd; margin-right: 8px; }
`

export const Input = styled.input`
  flex: 1;
  border: none;
  outline: none;
  font-size: 14px;
  color: #333;
`